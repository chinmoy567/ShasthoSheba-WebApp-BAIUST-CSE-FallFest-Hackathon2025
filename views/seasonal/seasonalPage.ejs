<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head", { title: "Seasonal Health Tips" }) %>
  <body class="bg-gradient-to-br from-gray-900 via-blue-900 to-indigo-900 min-h-screen text-gray-100">
    <%- include("../partials/navbar") %>

    <div class="max-w-4xl mx-auto mt-10 p-6 bg-blue-800/50 rounded-2xl backdrop-blur-sm border border-blue-600/30">
      <h1 class="text-3xl font-bold text-yellow-400 mb-4 text-center">
        <%= season %> Health Tips
      </h1>

      <p class="text-blue-200 text-center mb-6">
        Stay safe and healthy this <%= season %> with these preventive care tips.
      </p>

      <div class="space-y-6">
        <% tips.forEach(section => { %>
          <div class="bg-blue-900/40 p-5 rounded-xl shadow hover:shadow-lg transition border border-blue-600/30">
            <h2 class="text-xl font-semibold text-yellow-400 mb-2"><%= section.title %></h2>
            <ul class="list-disc list-inside text-blue-200">
              <% section.tips.forEach(t => { %>
                <li><%= t %></li>
              <% }) %>
            </ul>
          </div>
        <% }) %>
      </div>
    </div>

    <style>
      .card-shadow { box-shadow: 0 12px 30px rgba(14, 12, 26, 0.5); }
    </style>
  </body>
</html>
