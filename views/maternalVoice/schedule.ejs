<!DOCTYPE html>
<html lang="bn">
  <%- include('../partials/head', { title: 'মনোবন্ধু - স্বাস্থ্য সময়সূচি' }) %>
  <body class="bg-gradient-to-br from-gray-900 via-blue-900 to-indigo-900 min-h-screen text-gray-100">
    <div class="max-w-3xl mx-auto mt-10 bg-blue-800/50 backdrop-blur-sm p-8 rounded-2xl card-shadow border border-blue-600/30">
      <h2 class="text-2xl font-bold text-yellow-400 mb-6 text-center">
     মাতৃ ও শিশুর স্বাস্থ্য সময়সূচি
      </h2>

      <table class="w-full border-collapse">
        <thead>
          <tr class="bg-blue-700/50 text-left">
            <th class="p-3 border border-blue-600/30 text-yellow-400">পর্যায়</th>
            <th class="p-3 border border-blue-600/30 text-yellow-400">তারিখ</th>
            <th class="p-3 border border-blue-600/30 text-yellow-400">অবস্থা</th>
          </tr>
        </thead>
        <tbody>
          <% tracker.schedule.forEach(item => { %>
          <tr class="border border-blue-600/30 hover:bg-blue-700/30 transition">
            <td class="p-3 text-blue-200"><%= item.name %></td>
            <td class="p-3 text-blue-200"><%= new Date(item.date).toLocaleDateString('bn-BD') %></td>
            <td class="p-3 text-blue-200"><%= item.done ? ' দেওয়া হয়েছে' : ' অপেক্ষমাণ' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="text-center mt-6">
        <button
          onclick="speakTips()"
          class="bg-gradient-to-r from-green-500 to-green-600 text-gray-900 px-6 py-3 rounded-xl font-bold hover:from-green-400 hover:to-green-500 transition transform hover:scale-105 card-shadow border border-green-400"
        >
           পরামর্শ জানুন
        </button>

        <p id="tips" class="mt-4 text-blue-200"></p>
      </div>

      <div class="mt-6 text-center">
        <a href="/dashboard" class="text-yellow-400 hover:text-yellow-300 transition">🏠 মূল পেজে ফিরে যান</a>
      </div>
    </div>

    <script>
      function speakTips() {
        const text = "গর্ভবতী মা যেন পর্যাপ্ত বিশ্রাম নেন এবং প্রতিদিন প্রচুর পানি পান করেন।";
        const utterance = new SpeechSynthesisUtterance(text);
        utterance.lang = "bn-BD";
        utterance.rate = 1;
        speechSynthesis.speak(utterance);
        document.getElementById("tips").textContent = text;
      }
    </script>

    <style>
      .card-shadow { 
        box-shadow: 0 12px 30px rgba(14, 12, 26, 0.5); 
      }
    </style>
  </body>
</html>
