<!DOCTYPE html>
<html lang="bn">
  <%- include('../partials/head', { title: 'স্বাস্থ্য সময়সূচি' }) %>
  <body class="bg-gray-50">
    <div class="max-w-3xl mx-auto mt-10 bg-white p-8 rounded-2xl shadow-lg">
      <h2 class="text-2xl font-bold text-indigo-600 mb-6 text-center">
        🗓️ মাতৃ ও শিশুর স্বাস্থ্য সময়সূচি
      </h2>

      <table class="w-full border-collapse">
        <thead>
          <tr class="bg-indigo-100 text-left">
            <th class="p-3 border">পর্যায়</th>
            <th class="p-3 border">তারিখ</th>
            <th class="p-3 border">অবস্থা</th>
          </tr>
        </thead>
        <tbody>
          <% tracker.schedule.forEach(item => { %>
          <tr class="border hover:bg-gray-50">
            <td class="p-3"><%= item.name %></td>
            <td class="p-3"><%= new Date(item.date).toLocaleDateString('bn-BD') %></td>
            <td class="p-3"><%= item.done ? '✅ দেওয়া হয়েছে' : '⏳ অপেক্ষমাণ' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>

      <div class="text-center mt-6">
        <button
          onclick="speakTips()"
          class="bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700"
        >
          🔊 শুনুন স্বাস্থ্য টিপস
        </button>

        <p id="tips" class="mt-4 text-gray-700"></p>
      </div>

      <div class="mt-6 text-center">
        <a href="/dashboard" class="text-indigo-600 hover:underline">🏠 ড্যাশবোর্ডে ফিরে যান</a>
      </div>
    </div>

    <script>
      function speakTips() {
        const text = "গর্ভবতী মা যেন পর্যাপ্ত বিশ্রাম নেন এবং প্রতিদিন প্রচুর পানি পান করেন।";
        const utterance = new SpeechSynthesisUtterance(text);
        utterance.lang = "bn-BD";
        utterance.rate = 1;
        speechSynthesis.speak(utterance);
        document.getElementById("tips").textContent = text;
      }
    </script>
  </body>
</html>
