<!DOCTYPE html>
<html lang="bn">
  <%- include("../partials/head", { title: "উপসর্গ সচেতনতা গাইড" }) %>
  <body class="bg-gray-100 min-h-screen">
    <%- include("../partials/navbar") %>

    <div class="max-w-4xl mx-auto mt-10 p-6 bg-white shadow-lg rounded-xl">
      <h2 class="text-2xl font-semibold text-indigo-700 mb-4">
        💉 উপসর্গ সচেতনতা নির্দেশিকা
      </h2>
      <p class="text-gray-600 mb-4">
        আপনার উপসর্গ লিখুন (বাংলা বা ইংরেজিতে) এবং জানুন কখন ডাক্তার দেখানো
        প্রয়োজন:
      </p>

      <form id="symptomForm" class="space-y-4">
        <textarea
          id="message"
          rows="4"
          placeholder="যেমন: তিন দিন ধরে জ্বর আছে, মাথা ব্যথা করছে..."
          class="w-full p-3 border rounded-md focus:ring-2 focus:ring-indigo-400"
          required
        ></textarea>
        <button
          type="submit"
          class="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700"
        >
          বিশ্লেষণ করুন
        </button>
      </form>

      <div
        id="responseBox"
        class="hidden mt-6 bg-green-50 border border-green-300 p-4 rounded-lg"
      >
        <h3 class="text-lg font-semibold text-green-700">📋 বিশ্লেষণ ফলাফল:</h3>
        <p id="responseText" class="mt-2 text-gray-800"></p>
      </div>

      <% if (history && history.length > 0) { %>
      <div class="mt-10">
        <h3 class="text-lg font-semibold text-gray-700 mb-2">
          🕓 পূর্বের বিশ্লেষণ:
        </h3>
        <ul class="space-y-2">
          <% history.forEach(item => { %>
          <li class="border p-3 rounded-md bg-gray-50">
            <p><strong>🩺 উপসর্গ:</strong> <%= item.symptomText %></p>
            <p class="text-sm text-gray-600 mt-1">
              <%= item.aiResponse.substring(0, 100) %>...
            </p>
            <p class="text-xs text-gray-500 mt-1">
              📅 <%= new Date(item.createdAt).toLocaleString('bn-BD') %>
            </p>
          </li>
          <% }) %>
        </ul>
      </div>
      <% } %>
    </div>

    <script>
      const form = document.getElementById("symptomForm");
      const responseBox = document.getElementById("responseBox");
      const responseText = document.getElementById("responseText");

      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const message = document.getElementById("message").value;
        responseBox.classList.remove("hidden");
        responseText.innerText = "⏳ বিশ্লেষণ চলছে...";

        const res = await fetch("/dashboard/symptom/analyze", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ message }),
        });

        const data = await res.json();
        responseText.innerHTML = data.reply;
      });
    </script>
  </body>
</html>
