<!DOCTYPE html>
<html lang="en">
  <!-- ✅ Include shared head partial -->
  <%- include('partials/head', { title: title || 'মনোবন্ধু - বার্তা' }) %>

  <body
    class="bg-gradient-to-br from-gray-900 via-blue-900 to-indigo-900 min-h-screen flex flex-col text-gray-100"
  >
    <!-- ✅ Include shared navbar -->
    <%- include('partials/navbar') %>

    <!-- Message Container -->
    <div class="flex-grow flex justify-center items-center px-4">
      <div
        class="bg-blue-800/50 backdrop-blur-sm rounded-2xl p-10 w-full max-w-md text-center border border-blue-600/30 card-shadow transition transform hover:scale-[1.01]"
      >
        <!-- Title -->
        <h1 class="text-3xl font-bold text-yellow-400 mb-4">
          <%= title %>
        </h1>

        <!-- Message -->
        <p class="text-blue-200 mb-8 leading-relaxed"><%= message %></p>

        <!-- Buttons -->
        <div class="flex justify-center space-x-4">
          <% if (backLink) { %>
            <a
              href="<%= backLink %>"
              class="bg-gradient-to-r from-yellow-500 to-yellow-600 text-gray-900 px-6 py-3 rounded-xl font-bold hover:from-yellow-400 hover:to-yellow-500 transition transform hover:scale-105 card-shadow border border-yellow-400"
            >
              ← ফিরে যান
            </a>
          <% } %>
          <a
            href="/"
            class="bg-gradient-to-r from-blue-500 to-blue-600 text-white px-6 py-3 rounded-xl font-bold hover:from-blue-400 hover:to-blue-500 transition transform hover:scale-105 card-shadow border border-blue-400"
          >
            🏠 মূল পেজ
          </a>
        </div>
      </div>
    </div>

    <style>
      .card-shadow { 
        box-shadow: 0 12px 30px rgba(14, 12, 26, 0.5); 
      }
    </style>
  </body>
</html>