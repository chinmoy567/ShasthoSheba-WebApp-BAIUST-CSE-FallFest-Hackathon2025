<!DOCTYPE html>
<html lang="en">
  <!-- ✅ Include shared head partial -->
  <%- include('../partials/head', { title: 'মনোবন্ধু - আপনার প্রোফাইল' }) %>

  <body class="bg-gradient-to-br from-gray-900 via-blue-900 to-indigo-900 min-h-screen text-gray-100">
    <!-- ✅ Include shared navbar -->
    <%- include('../partials/navbar') %>

    <!-- Profile Section -->
    <section class="flex justify-center items-center py-16 px-4">
      <div class="bg-blue-800/50 backdrop-blur-sm rounded-2xl p-8 w-full max-w-md text-center card-shadow border border-blue-600/30 transition transform hover:scale-[1.01]">
        <h2 class="text-3xl font-bold text-yellow-400 mb-6">নিজ পরিচয়</h2>

        <% if (user) { %>
          <!-- ✅ User Info -->
          <div class="text-left space-y-3 mb-8">
            <p>
              <span class="font-semibold text-yellow-400">নাম:</span>
              <span class="text-blue-200 ml-1"><%= user.name %></span>
            </p>
            <p>
              <span class="font-semibold text-yellow-400">ই-মেইল:</span>
              <span class="text-blue-200 ml-1"><%= user.email %></span>
            </p>
          </div>

          <!-- ✅ Buttons -->
          <div class="mt-8 space-y-3">
            <a
              href="/"
              class="block bg-gradient-to-r from-yellow-500 to-yellow-600 text-gray-900 px-6 py-3 rounded-xl font-bold hover:from-yellow-400 hover:to-yellow-500 transition transform hover:scale-105 card-shadow border border-yellow-400"
            >
              ← মূল পেজে ফিরে যান
            </a>

            <a
              href="/auth/logout"
              class="block bg-gradient-to-r from-red-500 to-red-600 text-white px-6 py-3 rounded-xl font-bold hover:from-red-400 hover:to-red-500 transition transform hover:scale-105 card-shadow border border-red-400"
            >
              একাউন্ট থেকে বের হন
            </a>
          </div>
        <% } else { %>
          <!-- ⚠️ If user not logged in -->
          <div class="mb-8">
            <div class="w-16 h-16 bg-gradient-to-br from-yellow-500 to-yellow-600 rounded-2xl flex items-center justify-center mx-auto mb-4 metallic-effect">
              <span class="text-2xl text-gray-900">🔒</span>
            </div>
            <p class="text-blue-200 text-lg">
              নিজ পরিচয় দেখতে হলে নিজস্ব একাউন্ট দিয়ে প্রবেশ করা প্রয়োজন।
            </p>
          </div>
          <a
            href="/auth/login"
            class="bg-gradient-to-r from-yellow-500 to-yellow-600 text-gray-900 px-8 py-3 rounded-xl font-bold hover:from-yellow-400 hover:to-yellow-500 transition transform hover:scale-105 card-shadow border border-yellow-400"
          >
            এখানে ক্লিক করে প্রবেশ করুন
          </a>
        <% } %>
      </div>
    </section>

    <style>
      .card-shadow { 
        box-shadow: 0 12px 30px rgba(14, 12, 26, 0.5); 
      }
      .metallic-effect {
        background: linear-gradient(145deg, #eab308, #ca8a04, #eab308);
        box-shadow: 
          inset 0 2px 4px rgba(255, 255, 255, 0.4),
          inset 0 -2px 4px rgba(0, 0, 0, 0.3),
          0 4px 8px rgba(0, 0, 0, 0.3);
      }
    </style>
  </body>
</html>
