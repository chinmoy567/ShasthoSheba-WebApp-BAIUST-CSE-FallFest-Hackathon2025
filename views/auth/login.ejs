<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', { title: 'Login - ShasthoSheba' }) %>

  <body
    class="bg-gradient-to-br from-gray-900 via-blue-900 to-indigo-900 min-h-screen text-gray-100"
  >
    <%- include('../partials/navbar') %>

    <!-- Login Container -->
    <div class="flex justify-center items-center py-16 px-4">
      <div
        class="bg-blue-800/50 backdrop-blur-sm shadow-xl rounded-2xl p-8 w-96 border border-yellow-500/30 card-shadow"
      >
        <h2 class="text-3xl font-bold text-center text-yellow-400 mb-6">
          Login
        </h2>

        <!-- Login Form -->
        <form action="/auth/login" method="POST" class="space-y-4">
          <div>
            <label
              for="email"
              class="block text-sm font-medium text-blue-200 mb-1"
              >Email</label
            >
            <input
              id="email"
              type="email"
              name="email"
              required
              placeholder="Enter your email"
              class="w-full px-4 py-2 bg-blue-900/50 border border-blue-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 text-white placeholder-blue-300"
            />
          </div>

          <div>
            <label
              for="password"
              class="block text-sm font-medium text-blue-200 mb-1"
              >Password</label
            >
            <input
              id="password"
              type="password"
              name="password"
              required
              placeholder="Enter your password"
              class="w-full px-4 py-2 bg-blue-900/50 border border-blue-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500 text-white placeholder-blue-300"
            />
          </div>

          <button
            type="submit"
            class="w-full bg-gradient-to-r from-yellow-500 to-yellow-600 text-gray-900 py-3 rounded-lg font-bold hover:from-yellow-400 hover:to-yellow-500 transition metallic-effect transform hover:scale-105"
          >
            Login
          </button>
        </form>

        <!-- Extra Links -->
        <p class="text-center text-sm text-blue-300 mt-6">
          Don't have an account?
          <a
            href="/auth/register"
            class="text-yellow-400 hover:text-yellow-300 hover:underline"
            >Register here</a
          >
        </p>

        <!-- Back to home -->
        <p class="text-center text-sm text-blue-400 mt-4">
          <a
            href="/"
            class="hover:underline text-yellow-400 hover:text-yellow-300"
            >← Back to Home</a
          >
        </p>
      </div>
    </div>

    <style>
      .card-shadow {
        box-shadow: 0 12px 30px rgba(14, 12, 26, 0.5);
      }
      .metallic-effect {
        background: linear-gradient(145deg, #eab308, #ca8a04, #eab308);
        box-shadow: inset 0 2px 4px rgba(255, 255, 255, 0.4),
          inset 0 -2px 4px rgba(0, 0, 0, 0.3), 0 4px 8px rgba(0, 0, 0, 0.3);
      }
    </style>
  </body>
</html>
